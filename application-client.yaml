server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8765/eureka
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
    
security:
 oauth2:
   resourceserver:
    jwt:
     jwk-set-uri: http://localhost:9999/realms/real-estate-rental-agency-app/protocol/openid-connect/certs
